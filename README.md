Usa rand() com srand(time(NULL)) para gerar valores aleatórios diferentes a cada execução.

Tudo é feito com vetores e variáveis simples.

Cumpre todos os limites: até 200 produtos e até 50 clientes.

